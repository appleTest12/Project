<!DOCTYPE html>
<html lang="ko" xmlns:th="http://www.thymeleaf.org">
<head>
	<link rel="stylesheet" th:href="@{/css/notice.css}"/>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
  <div class="container">
  <h2>게시판 목록</h2>
    <table class="notice_list">
      <colgroup>
        <col width="15%"/>
        <col width="*"/>
        <col width="15%"/>
        <col width="20%"/>
      </colgroup>
      <thead>
        <tr>
          <th scope="col">글번호</th>
          <th scope="col">제목</th>
          <th scope="col">내용</th>
          <th scope="col">조회수</th>
          <th scope="col">작성일</th>
        </tr>
      </thead>
      <tbody>
        <tr th:if="${#lists.size(list)} > 0" th:each="list:${list}">
          <td th:text="${list.noticeId}"></td>
		  <td class="title">
          	<a href="openNoticeDetail?notice_id=" th:attrappend="href=${list.noticeId}" th:text="${list.title}"></a>
          </td>
          <td class="totle" th:text="${list.title}"></td>
          <td class="totle" th:text="${list.content}"></td>
          <td th:text="${list.hitCnt}"></td>
          <td th:text="${list.regDate}"></td>
        </tr>
        <tr th:unless="${#lists.size(list)} > 0">
          <td colspan="4">조회된 결과가 없습니다.</td>
        </tr>
      </tbody>
    </table>
    <a href="openNoticeWrite" class="btn">글 쓰기</a>
  </div>
</body>
</html>